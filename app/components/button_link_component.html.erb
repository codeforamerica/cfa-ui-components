<%= button_to @label, @url, class: "btn btn--large #{@button_style}", method: @method %>

<div x-data="{ count: 0 }">
  <button x-on:click="count++">Increment</button>

  <span x-text="count"></span>
</div>
