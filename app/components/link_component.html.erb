<span class='flex gap-cfa-sm'>
<% if @icon.present? %>
    <%= inline_icon(@icon, size: 20) %>
<% end %>
<%= link_to @url, target: "_blank", rel: "noopener noreferrer", **html_attrs do %>
    <%= @label %><span class="sr-only"> (opens in new tab)</span>
<% end %>
</span>
