# Disable automatic execution of dependency lifecycle scripts (pre/post install)
# to mitigate supply-chain attacks. See TEF-785.
# Project build scripts are still run explicitly via `yarn build` / `yarn build:css`.
ignore-scripts true
